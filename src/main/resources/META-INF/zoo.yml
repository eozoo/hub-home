logging:
  sql-enabled: false

mybatis-plus:
  global-config:
    banner: false

management:
  info:
    env:
      enabled: true
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: ALWAYS
  health:
    mail:
      enabled: false
    ldap:
      enabled: false
    ping:
      enabled: false
    diskSpace:
      enabled: false
